<div class="card mx-auto mb-4">
    <a href="">
        <div class="card-header">
            {{title}}
            <p class="author">Posted by {{user_name}} on {{format_date post.date_created}}</p>
        </div>
    </a>
    <div class="card-body">
        <p>{{text}}</p>
    </div>
</div>